import React, { FC } from "react";

const NavBar: FC = () => {
  return <div className={`h-1/4 bg-gray-800`}> 
    <div className={`text-white`}>
      Sign In
    </div>
  </div>;
};

export default NavBar;
